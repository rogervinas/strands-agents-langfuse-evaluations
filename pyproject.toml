[project]
name = "banking-sentinel"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "strands-agents[ollama,gemini,otel]==1.52.0",
    "strands-agents-tools==0.8.6",
    "langfuse==4.14.4",
    "fastapi==0.141.1",
    "uvicorn[standard]==0.52.3",
    "pydantic==2.13.4",
    "python-dotenv==1.2.3",
]

[project.optional-dependencies]
dev = [
    "pytest==9.1.1",
    "pytest-asyncio==1.4.0",
]
evals = [
    "strands-agents-evals==1.1.1",
    "httpx==0.28.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/banking_sentinel"]

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
